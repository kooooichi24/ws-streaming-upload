{
  "name": "ws-streaming-upload-backend",
  "version": "1.0.0",
  "description": "WebSocket streaming upload backend with Serverless Framework",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "remove": "serverless remove",
    "logs": "serverless logs -f",
    "invoke": "serverless invoke -f",
    "dynamodb:up": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml up -d dynamodb-local",
    "dynamodb:admin": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml up -d dynamodb-admin",
    "minio:up": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml up -d minio",
    "minio:logs": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml logs -f minio",
    "dynamodb:down": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml down",
    "dynamodb:logs": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml logs -f dynamodb-local",
    "dynamodb:admin:logs": "docker-compose -p ws-streaming-upload -f docker/docker-compose.yml logs -f dynamodb-admin",
    "dynamodb:create-table": "tsx scripts/create-table.ts",
    "minio:setup": "tsx scripts/setup-minio.ts",
    "test:websocket": "tsx scripts/test-websocket.ts",
    "offline": "IS_OFFLINE=true serverless offline",
    "offline:start": "IS_OFFLINE=true serverless offline start",
    "offline:full": "npm run dynamodb:up && npm run minio:up && sleep 5 && npm run dynamodb:create-table && npm run minio:setup && npm run offline"
  },
  "keywords": [
    "websocket",
    "serverless",
    "aws",
    "lambda"
  ],
  "author": "kooooichi24",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/ws": "^8.18.1",
    "serverless": "^4.23.0",
    "serverless-offline": "^14.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3",
    "ws": "^8.18.0"
  }
}
